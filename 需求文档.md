# 需求文档

## 1. 应用概述

**应用名称**：墨言

**应用描述**：一款基于网页的多人实时沟通工具，支持最多100台设备同时在线，提供屏幕共享、文字聊天、语音转文字、匿名模式和投票功能，无需下载安装，打开网页即可使用。界面呈现中国风美学，融入书法、山水画、大写意等传统艺术元素。

## 2. 用户与使用场景

**目标用户**：需要进行多人在线协作、会议、讨论的团队或群体

**核心使用场景**：
- 远程团队会议与协作
- 多人在线讨论与决策
- 需要匿名反馈的场景
- 需要屏幕演示的沟通场景

## 3. 页面结构与功能说明

### 3.1 页面结构

```
墨言
├── 首页
│   ├── 创建房间
│   └── 加入房间
└── 沟通房间页
    ├── 屏幕共享区
    ├── 文字聊天区
    ├── 参与者列表
    ├── 投票功能区
    ├── 导出功能
    └── 设置面板
```

### 3.2 功能说明

#### 3.2.1 首页

**创建房间**
- 用户点击「创建房间」按钮，系统生成唯一房间ID
- 显示房间链接，用户可复制分享给其他参与者

**加入房间**
- 用户输入房间ID或通过链接直接进入房间
- 输入昵称后进入沟通房间页

#### 3.2.2 沟通房间页

**屏幕共享区**
- 任意参与者可发起屏幕共享
- 仅提供共享整个屏幕选项，默认自动开启系统音频共享
- 支持最多100台设备同时查看共享屏幕并听到系统音频
- 显示当前共享者昵称
- 共享者可停止共享

**文字聊天区**
- 参与者可输入文字消息发送
- 参与者可点击语音输入按钮，系统将语音转录为文字后发送
- 消息显示发送者昵称和发送时间
- 支持匿名模式切换，匿名消息显示为「momo」

**参与者列表**
- 显示当前在线参与者昵称
- 显示在线人数
- 最多支持100台设备同时在线

**投票功能区**
- 任意参与者可发起投票
- 创建投票时设置投票标题、选项、投票模式（完全实名模式、完全匿名模式、混合模式）
- 完全实名模式：显示每个参与者的投票选择
- 完全匿名模式：仅显示投票结果统计，不显示投票者身份
- 混合模式：参与者可选择实名或匿名投票
- 投票结果实时更新
- 发起者可结束投票

**导出功能**
- 提供「导出聊天记录」按钮，点击后将文字聊天记录导出为本地文件（txt或html格式）
- 提供「导出投票结果」按钮，点击后将所有投票统计结果导出为本地文件（txt或html格式）

**设置面板**
- 参与者可切换匿名模式开关
- 匿名模式开启后，后续发送的消息显示为「momo」
- 匿名模式关闭后，恢复显示昵称

## 4. 界面风格设计

### 4.1 整体色调

**主色系**：水墨色系
- 深墨色：#1a1a1a ~ #2d2d2d
- 宣纸米白：#f5f0e8 ~ #ece5d4
- 朱砂红：#c0392b ~ #9b2335
- 青墨色：#2c5f6e
- 淡竹青：#8fbc8f

**色彩原则**：避免纯黑纯白，营造水墨晕染质感

### 4.2 背景设计

**首页背景**
- 融入淡雅的山水画轮廓（SVG或CSS绘制的远山、云雾、水波纹）
- 整体呈现留白之美

**房间页背景**
- 微妙的宣纸纹理（CSS grain/noise质感）
- 整体如宣纸铺陈

**装饰元素**
- 使用CSS实现毛笔水墨晕染效果的装饰元素（模糊的墨点、淡墨圆形）

### 4.3 字体与排版

**标题和重要标签**
- 使用衬线/仿宋/楷体风格
- 字体设置：font-family: 'Noto Serif SC', serif

**正文**
- 使用常规中文字体
- 保持书法韵律感

**应用名称**
- 采用大字书法题字风格展示

### 4.4 UI组件风格

**按钮**
- 圆角较小
- 边框细致，有类似印章感
- 主按钮：朱砂红配白字
- 次级按钮：墨色边框

**卡片/面板**
- 浅米白底色
- 带淡墨色细边框
- 圆角4~8px
- 有轻微纸张质感阴影

**输入框**
- 底部线条风格（underline style）
- 呼应毛笔落纸的线条感

**分隔线**
- 使用细墨线或竹节状装饰分隔

### 4.5 装饰元素

**关键区域装饰**
- 标题区、聊天区边角、空状态页加入水墨风格的SVG装饰
- 装饰元素包括：竹叶、梅花、印章、水波、云纹

**投票区**
- 使用印章风格标注「已投票」状态（圆形朱红印章样式）

**匿名用户头像**
- 使用水墨风格的圆形墨迹图案

**连接状态指示器**
- 用毛笔圆点代替普通圆点

### 4.6 动效

**消息出现动画**
- 从下方淡入
- 如墨汁渗入宣纸的晕染效果（fade + slight blur）

**按钮点击**
- 轻微的墨点扩散ripple效果

**加载状态**
- 用毛笔书写圆圈的旋转动画替代普通spinner

### 4.7 整体设计语言

**留白之美**
- 页面不过度填充，大量留白
- 如传统水墨画的构图美学

**功能区域布局**
- 主要功能区域清晰突出
- 辅助信息轻描淡写

**整体气质**
- 雅致沉静

## 5. 技术架构设计

### 5.1 连接架构

**文字聊天与信令层**
- 使用Supabase Realtime（WebSocket长连接）实现
- 所有消息经服务器中转，避免P2P Mesh网状结构导致的连接数爆炸

**屏幕共享方案**
- 采用截图定时同步方案
- 共享者发起共享时，以整个屏幕+系统音频模式调用getDisplayMedia
- 每隔500ms至1s截取屏幕帧（Canvas截图）
- 截图压缩后上传至Supabase Storage
- 通过Supabase Realtime广播最新截图URL
- 观看者订阅后实时拉取渲染
- 此方案避免WebRTC 1对N的P2P多路连接压力，适合100台设备同时观看

**媒体音频共享**
- 屏幕共享启动时，自动获取整个屏幕的视频和系统音频
- 音频通过WebAudio API采集后编码为小数据包
- 通过Supabase Realtime广播（或WebSocket）
- 避免WebRTC多路连接

### 5.2 断线重连与状态恢复

**房间状态持久化**
- 房间信息、在线成员列表、当前共享者、聊天记录、投票状态均存储于Supabase数据库

**重联恢复**
- 设备重新连接时，自动从数据库拉取完整房间状态（聊天历史、投票状态、最新屏幕截图）
- 无缝恢复到离线前的界面

**心跳机制**
- 客户端每30秒发送心跳更新在线状态
- 超过60秒未收到心跳则标记为离线
- Supabase Realtime自动处理重连

**连接状态提示**
- 界面显示连接状态（已连接/重连中/已断开）
- 重连成功后自动恢复订阅

### 5.3 低功耗优化

**屏幕共享截图帧率自适应**
- 当网络较慢时自动降低截图频率（从1fps降至0.5fps）
- 减少上传压力

**图片压缩**
- 截图压缩质量控制在60%至80% JPEG
- 限制分辨率最大1280x720

**非活跃优化**
- 页面不可见时（visibilitychange事件）暂停截图上传
- 恢复可见时继续

**内存管理**
- 聊天消息仅在内存中保留最近500条用于展示
- 全量记录用于导出

**避免轮询**
- 全部使用事件驱动（Supabase Realtime订阅）
- 严禁setInterval轮询数据库

### 5.4 稳定性保障

**重连退避**
- 断线后采用指数退避策略重连（1s、2s、4s、8s…最大30s）

**操作幂等**
- 所有写操作加乐观锁，避免重复提交

**房间容量控制**
- 数据库层面强制限制100台设备
- 超出时拒绝加入

### 5.5 屏幕共享截图传输优化算法

**脏矩形差量检测（Dirty Region Detection）**
- 共享端每帧截图后，与上一帧进行像素差量对比（使用Canvas getImageData对比）
- 仅上传发生变化的区域（脏矩形），而非全帧
- 屏幕静止时（差量低于阈值）停止上传，节省90%以上带宽
- 变化剧烈时（如视频播放）自动切换为全帧模式

**自适应帧率控制**
- 默认1fps，屏幕静止时降为0fps（停止上传）
- 检测到鼠标移动/窗口变化时提升至2fps
- 根据上一帧上传耗时动态调整下一帧间隔（反馈控制）
- 绝不超过2fps上限，防止设备过热

**图像压缩优化**
- 优先使用WebP格式（比JPEG小30%~50%），不支持时降级为JPEG
- 全帧模式：分辨率限制1280x720，质量60%
- 脏矩形模式：只压缩并上传变化区域，大幅降低数据量
- 观看端：仅在收到新截图URL时刷新，避免无效渲染

**观看端渲染优化**
- 使用CSS image-rendering: pixelated 保持清晰度
- 截图URL带时间戳参数防缓存，但复用同一 img 标签避免DOM重建
- 使用 requestAnimationFrame 渲染，避免强制同步布局

### 5.6 聊天消息与实时通信优化

**消息批处理**
- Supabase Realtime 原生支持，无需额外轮询

**虚拟滚动**
- 聊天列表使用虚拟滚动，仅渲染可见消息，防止DOM节点堆积导致内存泄漏

**防抖输入**
- 输入框防抖300ms，减少不必要的状态更新

### 5.7 客户端资源管理

**Web Worker 隔离**
- 截图差量计算放入 Web Worker，不阻塞主线程，防止UI卡顿

**内存上限管理**
- 内存中最多保留500条消息，超出自动释放旧数据（但持久化至Supabase供导出用）

**页面可见性优化**
- 切换到后台（document.hidden）时自动暂停截图采集，回到前台立即恢复

**截图对象释放**
- 每次生成的 ObjectURL / Blob 在发送后立即 revokeObjectURL 释放内存

### 5.8 Supabase Storage 管理

**截图文件命名**
- screenshots/{roomId}/latest.webp 固定路径，每次覆盖上传，避免文件堆积

**自动清理**
- 房间关闭或共享停止时，删除对应截图文件

**CDN缓存**
- 观看端通过附加 ?t=时间戳 参数绕过CDN缓存，获取最新截图

## 6. 业务规则与逻辑

### 6.1 设备连接规则
- 房间最多支持100台设备同时在线
- 超过100台设备时，新设备无法加入，提示「房间已满」
- 设备断线后可重新加入

### 6.2 屏幕共享规则
- 同一时间仅允许一个参与者共享屏幕
- 共享时仅提供共享整个屏幕选项，默认自动开启系统音频共享
- 其他参与者发起共享时，提示「当前已有人在共享屏幕」
- 共享者停止共享后，其他参与者可发起新的共享

### 6.3 语音转文字规则
- 参与者点击语音输入按钮后开始录音
- 录音结束后，系统将语音转录为文字
- 转录完成后，文字自动填入输入框，参与者可编辑后发送

### 6.4 匿名模式规则
- 匿名模式下，所有匿名用户统一显示名称为「momo」
- 匿名模式仅影响后续发送的消息，不影响历史消息
- 参与者可随时切换匿名模式

### 6.5 投票规则
- 每个参与者在同一投票中仅可投票一次
- 完全实名模式：所有参与者的投票选择公开显示
- 完全匿名模式：仅显示各选项的票数统计
- 混合模式：参与者投票时可选择实名或匿名投票，实名投票显示昵称，匿名投票不显示昵称
- 投票结束后，结果不可更改

### 6.6 导出规则
- 聊天记录导出包含所有消息内容、发送者昵称（匿名消息显示为「momo」）、发送时间
- 投票结果导出包含所有投票的标题、选项、各选项票数、投票模式信息
- 导出文件格式为txt或html

## 7. 异常与边界情况

| 场景 | 处理方式 |
|------|----------|
| 房间ID不存在 | 提示「房间不存在，请检查房间ID」 |
| 房间已满（100台设备） | 提示「房间已满，无法加入」 |
| 网络断线 | 提示「网络连接中断，正在重连」，重连成功后恢复 |
| 语音转文字失败 | 提示「语音识别失败，请重试」 |
| 屏幕共享冲突 | 提示「当前已有人在共享屏幕」 |
| 投票已结束 | 提示「投票已结束，无法投票」 |
| 无聊天记录时导出 | 提示「暂无聊天记录可导出」 |
| 无投票记录时导出 | 提示「暂无投票记录可导出」 |

## 8. 验收标准

1. 用户打开网页，点击「创建房间」，成功生成房间ID和链接
2. 其他用户通过链接加入房间，输入昵称后进入沟通房间页
3. 参与者发起屏幕共享（整个屏幕+系统音频），其他参与者可实时查看共享屏幕并听到音频
4. 参与者在文字聊天区发送消息，所有参与者可实时看到消息
5. 参与者切换匿名模式，发送的消息显示为「momo」
6. 参与者发起投票，选择投票模式，其他参与者可投票，投票结果实时更新
7. 参与者点击「导出聊天记录」，成功将聊天记录保存到本地电脑
8. 参与者点击「导出投票结果」，成功将投票统计结果保存到本地电脑

## 9. 本期不实现功能

- 用户注册与登录
- 视频通话功能
- 房间密码保护
- 房间管理员权限
- 消息撤回功能
- 表情包与富文本消息
- 私聊功能
- 屏幕共享录制功能